DECLARE X: INTEGER
DECLARE TEMP: INTEGER
DECLARE R: INTEGER
DECLARE RESULT: INTEGER

READ X
TEMP := X

WHILE X > 0
      R := X MOD 10
      RESULT := RESULT * 10 + R
      X := X / 10
END WHILE

IF TEMP = RESULT
      PRINT "PALINDROME"
ELSE      
      PRINT "NOT PALINDROME"
END IF
